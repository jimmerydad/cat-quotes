import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'org.romaine.app',
  appName: 'Cat Quotes',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
